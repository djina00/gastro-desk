<UserControl x:Class="GastroDesk.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GastroDesk.Views">

    <Grid Background="#F5F5F5">
        <Border Background="White"
                CornerRadius="10"
                Padding="40"
                Width="400"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.2"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="GastroDesk"
                           FontSize="32"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Foreground="#2196F3"
                           Margin="0,0,0,10"/>

                <TextBlock Text="Restaurant Management System"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           Foreground="#666"
                           Margin="0,0,0,30"/>

                <TextBlock Text="Username" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Padding="10"
                         FontSize="14"
                         Margin="0,0,0,15"/>

                <TextBlock Text="Password" Margin="0,0,0,5" FontWeight="SemiBold"/>
                <PasswordBox x:Name="PasswordBox"
                             Padding="10"
                             FontSize="14"
                             Margin="0,0,0,20"
                             PasswordChanged="PasswordBox_PasswordChanged"/>

                <Button Content="Login"
                        Command="{Binding LoginCommand}"
                        Height="45"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Background="#2196F3"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Margin="0,0,0,15"/>

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>

                <ProgressBar IsIndeterminate="True"
                             Height="4"
                             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"
                             Margin="0,10,0,0"/>

                <TextBlock Text="Default credentials: admin / admin123"
                           FontSize="11"
                           Foreground="#999"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
