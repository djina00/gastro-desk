<UserControl x:Class="GastroDesk.Views.OrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#F5F5F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- Orders List -->
        <Border Background="White" Margin="10" CornerRadius="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Orders" FontSize="18" FontWeight="Bold" Margin="15" Foreground="#333"/>

                <!-- Filter -->
                <StackPanel Grid.Row="1" Margin="15,0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <DatePicker SelectedDate="{Binding FilterDate}" Width="150"/>
                        <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="10,0,0,0" Padding="10,5"/>
                    </StackPanel>
                    <CheckBox Content="Show All Orders" IsChecked="{Binding ShowAllOrders}"/>
                </StackPanel>

                <ListBox Grid.Row="2"
                         ItemsSource="{Binding Orders}"
                         SelectedItem="{Binding SelectedOrder}"
                         Margin="10"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock FontWeight="SemiBold" FontSize="14">
                                            <Run Text="Order #"/>
                                            <Run Text="{Binding Id}"/>
                                            <Run Text=" - Table "/>
                                            <Run Text="{Binding TableNumber}"/>
                                        </TextBlock>
                                        <TextBlock Foreground="#666" FontSize="12">
                                            <Run Text="{Binding OrderDateTime, StringFormat='{}{0:MMM dd, HH:mm}'}"/>
                                        </TextBlock>
                                    </StackPanel>
                                    <Border Grid.Column="1" Padding="12,5" CornerRadius="10" Margin="10,0,0,0" VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#FFC107"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                        <Setter Property="Background" Value="#4CAF50"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                        <Setter Property="Background" Value="#f44336"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Status}" Foreground="White" FontSize="10" FontWeight="SemiBold" TextAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- New Order -->
                <StackPanel Grid.Row="3" Margin="15">
                    <TextBlock Text="New Order" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Table:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Text="{Binding NewTableNumber, UpdateSourceTrigger=PropertyChanged}"
                                 Width="60" Padding="8" Margin="0,0,10,0"/>
                        <Button Content="Create Order"
                                Command="{Binding CreateOrderCommand}"
                                Padding="15,8"
                                Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Order Details -->
        <Border Grid.Column="1" Background="White" Margin="10" CornerRadius="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Margin="15">
                    <TextBlock Text="Order Items" FontSize="18" FontWeight="Bold" Foreground="#333"/>
                    <TextBlock Foreground="#666" Margin="0,5,0,0">
                        <Run Text="Table "/>
                        <Run Text="{Binding SelectedOrder.TableNumber, FallbackValue='-'}"/>
                        <Run Text=" | "/>
                        <Run Text="{Binding SelectedOrder.Status, FallbackValue='-'}"/>
                    </TextBlock>
                </StackPanel>

                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding CurrentOrderItems}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          Margin="10"
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Dish" Binding="{Binding Dish.Name}" Width="*"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C2}" Width="80"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding Quantity}" Width="50"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding TotalPrice, StringFormat=C2}" Width="80"/>
                        <DataGridTemplateColumn Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="X"
                                            Command="{Binding DataContext.RemoveItemFromOrderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Background="#f44336" Foreground="White"
                                            BorderThickness="0" Width="30" Height="25"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <Border Grid.Row="2" Background="#E3F2FD" Padding="15" Margin="10,0">
                    <TextBlock FontSize="18" FontWeight="Bold">
                        <Run Text="Total: "/>
                        <Run Text="{Binding CurrentOrderTotal, StringFormat=C2, Mode=OneWay}"/>
                    </TextBlock>
                </Border>

                <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="15" HorizontalAlignment="Right">
                    <Button Content="Complete Order"
                            Command="{Binding CompleteOrderCommand}"
                            Padding="15,10" Margin="0,0,10,0"
                            Background="#4CAF50" Foreground="White" BorderThickness="0"/>
                    <Button Content="Cancel Order"
                            Command="{Binding CancelOrderCommand}"
                            Padding="15,10" Margin="0,0,10,0"
                            Background="#f44336" Foreground="White" BorderThickness="0"/>
                    <Button Content="Delete Order"
                            Command="{Binding DeleteOrderCommand}"
                            Padding="15,10"
                            Background="#9E9E9E" Foreground="White" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Available Dishes -->
        <Border Grid.Column="2" Background="White" Margin="10" CornerRadius="5">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Add Items" FontSize="18" FontWeight="Bold" Margin="15" Foreground="#333"/>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding AvailableDishes}"
                         SelectedItem="{Binding SelectedDish}"
                         Margin="10"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="8">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                                        <TextBlock Text="{Binding Category.Name}" Foreground="#666" FontSize="11"/>
                                        <TextBlock Text=" - " Foreground="#666" FontSize="11"/>
                                        <TextBlock Text="{Binding Price, StringFormat=C2}" Foreground="#2196F3" FontWeight="SemiBold" FontSize="11"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="2" Margin="15">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Quantity:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Text="{Binding QuantityToAdd, UpdateSourceTrigger=PropertyChanged}"
                                 Width="50" Padding="8"/>
                    </StackPanel>
                    <Button Content="Add to Order"
                            Command="{Binding AddItemToOrderCommand}"
                            Padding="15,10"
                            Background="#2196F3" Foreground="White" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading Indicator -->
        <Border Grid.ColumnSpan="3" Background="#80FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="10"/>
                <TextBlock Text="Loading..." Margin="0,10,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Error Message -->
        <Border Grid.ColumnSpan="3" VerticalAlignment="Bottom" Margin="20"
                Background="#FFF3E0" CornerRadius="5" Padding="15"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#E65100"/>
        </Border>
    </Grid>
</UserControl>
